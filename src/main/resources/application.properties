spring.application.name=multi-file-processing

#spring.config.import=secret.properties

spring.batch.job.enabled=false
spring.batch.jdbc.initialize-schema=always
spring.jpa.hibernate.ddl-auto=create-drop

input.folder.vehicles=classpath:/data/*.csv
input.folder.vehicles.json=classpath:/data/*.json
input.folder.vehicles.pdf=classpath:/data/*.pdf
spring.profiles.active=pdf

spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=""
spring.mail.password=""
spring.mail.properties.mail.smtp.auth=false